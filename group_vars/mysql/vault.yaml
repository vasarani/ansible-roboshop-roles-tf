MYSQL_ROOT_PASSWORD: "{{ (lookup('amazon.aws.aws_ssm', '/roboshop/dev/mysql_root_password', region='us-east-1', decrypt=True) | from_json).MYSQL_ROOT_PASSWORD }}"
#"{{ lookup('amazon.aws.aws_ssm', '/roboshop/dev/mysql_root_password', region='us-east-1', decrypt=True) }}"

